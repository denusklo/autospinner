# LESSONS — Pitfall Records (append-only)

> Write format and compaction rules in `04-KNOWLEDGE-PROTOCOL.md` sections 2 and 4. When an error feels familiar, scan this file's titles first.
> L1–L5 are seed entries backfilled on 2026-07-07 from the 2025-11 development history (~/.claude/history.jsonl); dates are approximate.

## L1 2025-11 Picked the wrong canvas: the page has both BarCanvas and DragCanvas
- Symptom: cannot read the board / all read pixels are wrong
- Root cause: the page has two canvases and the wrong one was picked at first; the correct target is DragCanvas
- Lesson: next time on a canvas-based page, enumerate all canvases (id, size) before choosing; do not assume there is only one
- Evidence: content.js:49-91 now matches DragCanvas first, falling back to largest area
- Affected files: PROJECT-FACTS F3

## L2 2025-11 Guessing the Light rune's color signature was wrong; live measurement was right
- Symptom: Light runes persistently misread as other types
- Root cause: the model inferred "yellow is probably (255,255,0)"; the measured cell-center pixel is (136,85,0)
- Lesson: next time for color/pixel/coordinate constants, always demand measured values (have the User run a sampling log); writing "common-sense values" into code is forbidden
- Evidence: content.js:182 `Light, r:136, g:85, b:0` annotated as live-measured calibration
- Affected files: PROJECT-FACTS F4

## L3 2025-11 Dragging too fast drops the rune early
- Symptom: path planned 0,0→2,0 but the rune actually dropped halfway; the User asked back "maybe you are moving too fast?"
- Root cause: mousemove point spacing too large / frequency wrong; the simulator judged a release
- Lesson: next time with synthetic-event-driven UI, treat "event interval and step distance" as first-class parameters, observable via logs; do not hard-code and forget
- Evidence: content.js:413-441 is now 16ms point-by-point mousemove
- Affected files: PROJECT-FACTS F6/F7

## L4 2025-11 Global-variable archaeology failed; ended up with canvas pixel reading
- Symptom: `window.board / boardData / gameBoard...` all missing; multiple rounds of attempts fruitless
- Root cause: the simulator does not expose board globals (probably inside a closure)
- Lesson: next time "guessing page-internal variable names" goes one round without result, switch immediately to external observation (pixels/DOM); do not keep expanding the guess list
- Evidence: content.js:246-259 keeps Method 1 as fallback; the main path is now readBoardStateFromCanvas
- Affected files: none

## L5 2025-11 The human-pasted-log blind loop is the biggest token black hole (meta-lesson)
- Symptom: many rounds of "User pastes 100+ console lines → model guesses → changes → paste again"; the same symptom iterated 3+ rounds
- Root cause: the model cannot see the browser and had no structured reporting contract, effectively using the User as a low-frequency, high-noise sensor
- Lesson: next time for any browser-side change, first establish the `[TOS] KEY=value` single-line log contract (CLAUDE.md R3), and prefer moving extractable logic into algorithm.js for Node verification
- Evidence: 00-DIAGNOSIS.md section 2 (history.jsonl sampling)
- Affected files: none (already institutionalized as R2/R3)

## L6 2026-07-07 Post-execution logger compared incomparable scoring scales
- Symptom: User saw "Expected score: 42, Actual score: 250, Difference: 208" and could not tell whether the run succeeded. It had in fact executed perfectly (verified by Node replay of the logged path: 27/30 cells matched; the 3 mismatches were dimmed Dark runes misread as Fire during the clear animation).
- Root cause: logFinalBoardState still used the legacy MatchFinder score (points scale) while the solver reports DoraSolver weight (different unit); it also used the double-counting legacy combo counter, so "Light x4 + Light x3" were the same four runes counted twice.
- Lesson: next time two scoring systems coexist, never print them in the same comparison — compare boards cell-by-cell (planned vs read), not scores. When a metric changes, grep for every consumer of the old metric in the same commit.
- Evidence: content.js logFinalBoardState (now rewritten to emit [TOS] RESULT with execErrors vs animArtifacts); user log 2026-07-07.
- Affected files: PROJECT-FACTS F4 updated (confirmed Dark/Fire r=153 misread pair).

## L7 2026-07-07 PowerShell `>` corrupts binary stdout from native commands (adb screencap)
- Symptom: `adb exec-out screencap -p > file.png` produced a 5,223,576-byte file for what is a ~2.7MB PNG (≈2x size); PNG unreadable.
- Root cause: Windows PowerShell 5.1 redirection decodes native stdout as text and re-encodes as UTF-16LE, doubling/mangling every byte. Not adb's fault.
- Lesson: next time binary data must cross adb, never use PowerShell `>`; write to a device file then `adb pull` (or spawn adb from Node with stdout piped to a file).
- Evidence: corrupted pull 5223576 bytes vs `adb pull` clean 2745303 bytes, session 2026-07-07.
- Affected files: PROJECT-FACTS P4 records the safe capture path.

## L8 2026-07-07 Extrapolated color signature silently misread Light+thorn as Fire+thorn (repeat of L2's root cause)
- Symptom: `--rounds 2` round 2 timed out "board did not settle"; dry run showed (5,0) read as Fire* while the screen showed a gold Light+thorn rune, and (2,2) UNKNOWN d=78 (a new "enhanced" white-sparkle Water variant).
- Root cause: two thorn signatures were EXTRAPOLATED (base×0.55) instead of measured; actual Light+thorn (118,98,52) landed nearer Fire+thorn (129,61,53) than the guess (107,79,6). A wrong signature fails silently; only a missing one triggers the refuse-to-spin guard.
- Lesson: next time a color variant has not been observed live, leave it OUT of SIGNATURES so the unknown-guard fires and prints the measured rgb/dark% — never ship a guessed constant (same rule as L2, now for the phone channel).
- Evidence: [TOS] UNKNOWN_CELLS=(2,2)d=78rgb=118,193,239; board-sample.js row0 col5=(118,98,52)d37, session 2026-07-07.
- Affected files: PROJECT-FACTS P5 updated (measured Light+thorn + enhanced Water; missing-variant policy).

## L9 2026-07-07 Fixing timer precision broke the drag — the old slack was load-bearing
- Symptom: after replacing sleep-per-point (Windows overshoot → real ~35ms/point) with deadline scheduling (exact 16ms/point), the real game dropped the held rune after 3-4 of 23-29 planned moves (counter deltas 500→497, 497→493). The identical nominal parameters had always worked before.
- Root cause: the game drops the rune on large per-event jumps (36px at fast cadence). The sloppy timers accidentally kept the effective speed at ~150-190ms/move, inside the game's tolerance; precise 80ms/move at 36px steps is outside it. Same 80ms/move with 18px steps (10 points/cell) executed a full 24-move first=5 path and cleared the stage.
- Lesson: next time timing/precision infrastructure improves, re-verify every empirically-tuned threshold that was calibrated under the old behavior (P7 counter check per speed change); and when a drag speed fails, try finer interpolation before slowing down — the threshold is per-event distance, not path speed.
- Evidence: [TOS] SPIN msPerMove=50/80 + counter screenshots 500→497→493; fine-step run cleared the stage, session 2026-07-07.
- Affected files: PROJECT-FACTS P8 rewritten (speed/step-distance table).

## L10 2026-07-07 L9's step-distance hypothesis was wrong — the real culprit was adb transport bursts (fix: pace on the device)
- Symptom: fine 18px steps @ 80ms/move (L9's fix) also dropped the rune (5/28 moves, counter 500→495); worse, undelivered touch events from killed runs replayed later as phantom moves (next counter delta 27 for a 15-move path).
- Root cause: PC-side pacing (sleep AND deadline-scheduled) is meaningless because stdin-over-adb/USB coalesces writes into bursts; the game sees stalls + clustered jumps. Event timing must be enforced where events are injected.
- Lesson: next time timing matters across a transport, embed the timing IN the payload (minitouch `w <ms>` waits, whole script sent upfront) instead of pacing the sender; and after killing a touch-injection process, assume leftover events may still fire — re-verify the counter baseline before the next measurement.
- Evidence: device-paced runs counter-exact 29/29 @80ms and 27/27 @50ms (468→439→412) vs PC-paced 5/28; session 2026-07-07.
- Affected files: PROJECT-FACTS P8 rewritten (device-paced dispatch is now the hard rule).

## L11 2026-07-07 MaaTouch has no `w` wait — the "device-paced timing" of L10 never existed (and the kill-timer caused stuck touches)
- Symptom: rune occasionally stayed held after a finished spin. Investigation: a script of `w 100`x20 (2s nominal) finished in ~0ms; `w 2000` between down/up executed instantly (process exit 264ms).
- Root cause: MaaTouch v1.1.0 ignores `w` — every "paced" script actually injected contiguously at driver speed (the game accepts this; that's why counters matched exactly). The stuck touch was the p.kill() timer racing MaaTouch's boot: killing the adb session discards unconsumed script including the final `u 0`. Also: stdin EOF kills the remote mid-script (102ms for a 2s script), so streaming+closing is equally unsafe.
- Lesson: next time timing/protocol behavior is assumed, MEASURE it before building on it (a wait that doesn't wait is silent); for run-to-completion semantics over adb, redirect stdin from a device FILE and treat process self-exit as the completion proof — never a kill timer, never stdin EOF.
- Evidence: maat-w-test.js exit 264ms code=137; EOF test exit 102ms; L9/L10's "speed limit" narrative retired.
- Affected files: PROJECT-FACTS P8 rewritten again; phone/autospin.js executeTouchPath rewritten (file-redirect + self-exit + rescue-release).

## L12 2026-07-07 Truncated drags were ELECTRIC-RUNE interrupts, not touch timing — and flickering rune states need multi-frame recognition
- Symptom: in 星斗珠盤·火, every drag registered only 0-4 of 12-29 planned moves across three different dispatch designs (instant, PC-paced, file-scripted), spawning two wrong theories (L9 speed limits, frame sampling). Meanwhile a 2-move spin registered ZERO.
- Root cause: the stage's electric runes interrupt the spin when touched or passed through, and their flickering white-cyan glow classified as "Water"/"enhanced Water", hiding them from recognition. Every truncation point mapped exactly onto a misread electric cell — including the 0-move run which STARTED on one. Additionally, single-frame hue voting misidentified the base element ~40% of frames (arcs cover all sub-patches in some frames).
- Lesson: next time drags truncate at consistent counts, map the truncation cells against the recognized board BEFORE touching dispatch code — an interrupting rune state is one misread away; and for any animated/flickering rune state, pool recognition evidence across multiple frames instead of trusting one capture.
- Evidence: truncation analysis (paths hit "Water" at exactly moves 4/3/0); flicker probe (wood cells read pure blue in 2 of 5 frames); post-fix live spin 29/29 moves + first-wave electric clear + 161M damage, 2026-07-07.
- Affected files: PROJECT-FACTS P11 added; P8's pacing rationale corrected in code comments.

## L13 2026-07-07 Arc-flare bleed created GHOST electric detections one cell off — the drag then hit the real, unmarked shock
- Symptom: User watched the hand grab/pass shock runes although every planned path avoided all recognized electric cells. Consecutive runs "saw" the shock at (3,1), then (4,1), then (4,2) — interpreted first as the shock moving (it never moves except gravity/touch — User stated).
- Root cause: electric arcs flare BEYOND the cell border; a single-frame whiteness test sometimes fires on the neighbor while the true shock's own center reads dim that frame. Recognition then marked the neighbor, left the real shock unmarked, and the solver routed straight through it. 8-frame probe: real shocks are white ~8/8 frames; ghosts are 1-frame transients.
- Lesson: next time a rune STATE is detected from glow/animation, require PERSISTENCE across frames (white in >=2 of 3), not presence in one — union alone imports ghosts, single-frame alone mislocates. And when "the obstacle moved" contradicts the game's rules, suspect the detector before inventing new mechanics.
- Evidence: multi-shock-probe.js — 8/8 frames electric={(4,2),(1,3),(2,3)}, (3,1)=(30,178,39) pure Wood every frame; user runs 1-2 had paths crossing the unmarked (4,2) 3x/2x, 2026-07-07.
- Affected files: PROJECT-FACTS P11 updated (persistence rule, relaxed thresholds g>215/b>205).

## L14 2026-07-08 Dark-type shocks (magenta arcs) invisible to the green-based whiteness rule — needed element-agnostic detection
- Symptom: dark shock runes at (4,0)/(4,2) read as Heart; board was heart-heavy and the plan built on a wrong board (user caught it before Enter).
- Root cause: electric detection required high GREEN (assumed cyan arcs). Dark shocks glow MAGENTA-white (r+b high, green swings 135-207), failing the g>215 test. Element-specific rule doesn't generalize across shock base colors.
- Lesson: next time detecting an animated overlay across variants, key on the INVARIANT (electric = ≥2 channels near-white + frame flicker >25, since arcs of any hue light ≥2 channels and all normal/enhanced runes are frame-stable ≤15) rather than one color family. The flicker test also cleanly rejects the enhanced-Dark near-miss (225,77,237) that passes the ≥2-channel candidate rule.
- Evidence: dark-shock-probe.js — (4,0)/(4,2) swing (221,135,254)↔(243,207,254), frame delta 50-59; all other cells delta 0-15; darkbase-probe darkest patches (50,20,82)(79,37,148) → purple family. Post-fix dry run: both read Dark^, path avoids both, 2026-07-08.
- Affected files: PROJECT-FACTS P11 rewritten (element-agnostic candidate + flicker confirmation + purple-family base rule).

## L15 2026-07-08 Light-type shock bases are pixel-unreadable (glare); use frame-CONSISTENCY to refuse, plus a manual override
- Symptom: 3 Light shocks read as Water/Dark/Fire across successive captures (all wrong; all are Light). Adding a yellow-detection rule made it confidently wrong instead of refusing.
- Root cause: a Light shock's white-cyan-yellow glare is so bright the darkest sub-patches still saturate; the residual base tint flips family frame-to-frame. No single-capture color rule can separate "wood shock" from "light shock that looks woodish this frame."
- Lesson: when a signal is intrinsically unstable, the reliable discriminator is CONSISTENCY not a better threshold — classify each frame independently and refuse (-1) unless frames agree (verified-stable Wood/Water/Dark agree every frame; Light/Fire flip). Pair auto-detect with a manual override for the unreadable cases rather than chasing a perfect heuristic (R4: stopped after the yellow-rule strike). Also: overrides applied AFTER waitForStableBoard must be tolerated BY it (allowUnknownShocks) or live mode dead-ends on the refuse.
- Evidence: light-shock-probe two runs gave blue-dominant then yellow-dominant for the same cell; --shock-bases l live spin counter 338→316 (exact 22 moves), 2026-07-08.
- Affected files: phone/autospin.js (per-frame electricBase + --shock-bases + waitForStableBoard allowUnknownShocks); PROJECT-FACTS P11.

## L16 2026-07-08 TEMPORAL MINIMUM cracks the "unreadable" bright shock — arcs are additive, so min-over-frames reveals the base
- Symptom: L15 concluded Light shock bases were intrinsically unreadable (refuse + manual override). User pushed: "can we extract the signature? take as many screenshots as you want."
- Root cause of earlier failure: single/few-frame reads see base+arc (additive glare) which flips the apparent hue. The base is invariant; the glare is what varies.
- Lesson: for an ADDITIVE transient overlay (glow, glare, specular flicker), take the per-pixel TEMPORAL MINIMUM across many frames — it strips the overlay and leaves the base, where a few frames' average or consistency-vote cannot. Then subtract the lowest channel to remove any residual white floor and read the pure hue. This turned all 6 shock bases from "refuse" into reliable auto-detection. Frame count matters: min must converge (10 too few → Light residual g/r sinks toward Fire; ~18-20 stable).
- Evidence: temporal-min bases separated all 6 elements cleanly (Wood(22,121,16)…Light(102,61,1)…); Light shocks residual g/r~0.45 vs Fire 0.11; live auto both shocks Light^ weight 91, 2026-07-08.
- Affected files: phone/autospin.js electricBase rewritten (temporal min + hue tree), 18-frame burst; PROJECT-FACTS P11.

## L17 2026-07-08 "Too fast drops the rune" was a stdin-flush bug, not a game speed limit — busy-wait starved libuv so events shipped as one burst
- Symptom: --move-ms 200 (20ms/pt) "untraceable" drop; 210 (21ms/pt) worked. Looked like a sharp game threshold at ~205ms/move.
- Root cause: executeTouchPath busy-waited to each deadline. When per-point lead <= 20ms (i.e. fast speeds) the `if(lead>20) sleep` branch was skipped, so the loop NEVER awaited — libuv never flushed the stdin pipe, Node batched every write, and adb delivered them to the device as a 0.4ms burst (measured via dumpsys RecentQueue). The game saw one instant jump and dropped the rune. At 21ms the sleep branch fired, yielding/flushing per point (device spacing 19ms).
- Lesson: a CPU busy-wait in an async pipeline silently blocks I/O flushing — if you must spin to a deadline, YIELD each spin (`await new Promise(setImmediate)`) so pending writes drain. Verify transport timing at the DESTINATION (dumpsys event ages), not just the sender's write timestamps — they diverged completely (PC 20ms, device 0.4ms).
- Evidence: timing-inspect.js — stepMs=20 device spacing 0.4ms (bug) → 17.9ms (after setImmediate yield); PC-side spacing 20.0ms in both.
- Affected files: phone/autospin.js executeTouchPath busy-wait now yields via setImmediate; PROJECT-FACTS P8.

## L18 2026-07-08 Enhanced (white-sparkle) Heart misread as an electric rune, blocking --start on it
- Symptom: `--start=2,1` aborted "start-unpickable" — (2,1), a normal Heart, was recognized as `Heart^` (electric), which sets NO_PICKUP. User confirmed it is a plain Heart.
- Root cause: TWO bugs stacked. (1) The electric confirmation `candFrames>=2 && (maxDelta>25 || meanMin>90)` fired on `meanMin>90` even with no flicker (maxDelta=7). An enhanced Heart measured (244,172,210): a glow candidate (r,b over threshold) with all channels high — the `meanMin>90` fallback wrongly assumed "no enhanced rune lifts all channels". (2) After removing that flag, the not-electric fallback called `classify()`, whose own `isElectricGlow` early-return hands back the type:0 placeholder → the Heart then misread as **Water**.
- Lesson: flicker (maxDelta) is the ONLY reliable electric discriminator — never infer electric from brightness/meanMin (enhanced runes are bright too). And a helper that returns a placeholder type behind a guard must expose a way to bypass that guard (classify(stats, skipGlow)) or the placeholder leaks wherever the guard is re-checked. Verify recognition fixes on the LIVE board with a burst probe (probe-electric.js pattern), not by reasoning about thresholds.
- Evidence: probe on live board — (2,1) mean=(244,172,210) maxDelta=7 candFrames=18/18; real Hearts (229,92,168) maxDelta≤3; after fix BOARD_ROW1 reads ...,Heart,... and --start=2,1 --end=4,4 solves start=2,1 end=4,4.
- Affected files: phone/autospin.js classify(skipGlow) + electric decision (flicker-only); PROJECT-FACTS P5/P11.
- ⚠️ UNVERIFIED after fix: real electric-rune detection (no shock on the test board). Flicker-only matches P11's measured ~60/frame swing, but confirm on a real shock stage next time one is loaded.

## L19 2026-07-08 "Bigger beam → fewer combos" was a measurement artifact, not a bug
- Symptom: `--clear-all heart` + both start/end pins returned only 1 combo. A beam sweep looked pathological — 200→5, 800→6, 1600→1 combos ("wider beam does worse", which should be impossible) — screamed bug. Chased it with a scoring change (clearAllMetBonus) that changed nothing.
- Root cause: DoraSolver returns `pick = bestQualified ?? best`. When nothing satisfies the hard demand (clear-all + both pins), it falls back to `best`, which IGNORES the demand. The small-beam "5/6 combo" results were UNMET fallbacks autospin would then ABORT — not valid solutions. Comparing raw comboCount without checking constraint satisfaction compared real clear-all solutions against fallbacks. Measured correctly (firstClearedByType>=total): monotonic — beam 1600→1, 8000→3, 16000→4, all clearing all hearts. No bug; a tight constraint just needs a wide beam.
- Lesson: when A/B-ing solver outputs, ALWAYS report constraint-satisfaction next to the objective — a `bestQualified ?? best` fallback silently violates the demand and out-scores real solutions on the raw metric. One wasted (reverted) code change came from trusting comboCount alone. Confirm the metric measures what you think before theorizing a bug (02 matrix R1-3).
- Evidence: scratchpad clean-evidence.js — beam800 "6c" flagged clearsAllHearts=false (FALLBACK); beam1600/8000/16000 = 1/3/4c all clearsAllHearts=true (1.9s/24s/48s).
- Affected files: none (clearAllMetBonus experiment reverted; algorithm unchanged) — behavioral note only.

## L20 2026-07-08 clear-all MISS is a beam-width problem, NOT a planner problem — and the abort hint was backwards
- Symptom: `--clear-all dark` + `--end 5,0` MISSed (3/4 darks) at beam 3200→5000 and maxPath 200→400 (one run took 168s). The abort message advised "try --beam 800 --max-path 40" — smaller, which never helps.
- Root cause: clearing all 4 scattered darks needs them gathered into one dissolving line; that clear-all-satisfying state only enters the beam above a width threshold. Below it, DoraSolver returns the fallback `best` (9 combos, 3/4 — aborts). Beam 8000 crosses the threshold: 4/4 cleared AND 8 combos, maxPath 60, ~27s. A long maxPath (400) does NOT help clear-all and costs ~10x time.
- Tried and REVERTED (two-strikes): (1) clearAllMetBonus feasibility-gate rescoring — neutral. (2) Extending TargetPlanner to CONSTRUCT clear-all coverage targets (straight dark-lines + extra triples) then route them — routeToTarget can't fully realize a multi-cell coverage target AND land on the end pin (routing-failed on 24/24 targets, 175s). The construct-then-route approach hits a routing ceiling with pins; not viable without a denser router.
- Lesson: for a clear-all MISS, raise --beam (8000, then 16000) and keep --max-path ~60 — do not reach for the planner or a longer path. When DoraSolver "misses a hard demand", first check whether a wider beam simply crosses the threshold before building new machinery (cheaper and it worked here). Always give correct escalation advice in abort messages — the wrong hint cost real user time.
- Evidence: scratchpad clearall-what-works.js / maxpath-test.js — end 5,0: beam 5000/maxPath 400 → 3/4 (168s); beam 8000/maxPath 60 → 4/4 + 8 combos (27s). Planner end-pin: routing-failed 24 targets, 175s.
- Affected files: phone/autospin.js clear-all abort hint corrected (→ wider beam, keep maxPath 60); algorithm.js unchanged (both experiments reverted). ⚠️ SUPERSEDED IN PART by L22: for badly-scattered scarce types a wider beam is NOT enough — the coverage planner (now wired) is required and works. "Don't use the planner" applies only to mildly-scattered cases.

## L21 2026-07-08 clear-all CLUMPS runes into one big group (1 combo) — split them with --first-combos N+
- Symptom: `--clear-all dark` (6 darks) cleared all 6 but as ONE 6-rune group = 1 combo, "wasting" a combo (two 3-groups would be 2 combos / more damage). A wider beam (8000, 16000) did NOT split them.
- Root cause: the clear-all steering rewards required runes cleared AND adjacent same-color pairs (pairPotential), so it actively CLUMPS the darks together (more adjacency = higher score); the scoring only mildly prefers 2 groups (+2 weight vs the 6-group's size bonus), not enough to overcome the clumping bias, and the split arrangement is more constrained so the beam doesn't stumble onto it.
- Lesson: to make clear-all produce MULTIPLE combos from the required color, add `--first-combos N+` — it rewards first-wave combo COUNT, which is the "prefer more groups" signal clear-all lacks. Measured: `--clear-all dark --first-combos 5+` split a [6] blob into [3,3] and raised total combos 6→8 (and was faster). This is a flag combination, not a code fix — reach for it before touching solver scoring.
- Evidence: scratchpad dark-firstcombos.js — clear-all only: total6 first3 darks[6]; +first-combos 5+: total8 first6 darks[3,3]; CLI --board dry-run reproduced (8 combos, Dark 6/6 ok, end 5,3).
- Affected files: phone/autospin.js --clear-all help text (usage tip added); no logic change.

## L22 2026-07-08 The constructive clear-all planner DOES work — but only COVERAGE-ONLY (supersedes L20's "don't use the planner" and the earlier "abandoned" note)
- Context: L20 concluded a clear-all MISS is only a beam-width problem and "don't reach for the planner". That holds for a MILDLY scattered scarce type (4 darks → wider beam gathers them). It is FALSE for a badly scattered one: 5 Waters spread to all corners gave 3/5 at beam 8000 AND 16000, no pins — a wider beam never crosses the threshold because 5 can only clear as ONE connected 5-group and the beam won't gather 5 corner runes.
- What actually failed the first two planner attempts: (1) targets were AUGMENTED with extra combo triples → too many cells for routeToTarget to realize + land on the end pin; (2) augmented targets were tried FIRST, starving the routing budget before the working bare-coverage ones; (3) a bug — `this.target` (the combo-count planner's floor) defaults to 5, so solveClearAll silently required firstCombos>=5 and discarded every 1-3-combo clear-all solution as "routing-failed".
- The fix (WORKS): construct COVERAGE-ONLY targets — just the required runes as straight dissolving lines, NO extra combo triples — route many placements, keep the max-combo routed one; add a separate `clearAllComboFloor` (0 unless --first-combos given). Coverage-only targets route reliably WITH the end pin (8/8 water-5-line placements routed to 5,0). autospin now escalates clear-all MISSes to it. Result on the 5-water board: 5/5 cleared + 3 combos + end pin (DoraSolver got 3/5). Slow (~100-150s: it routes every placement to maximize combos) but achieves the goal.
- Lesson: a "construct then route" planner is viable ONLY if the target is MINIMAL (cover the demand, nothing more) — every extra target cell multiplies routing difficulty, especially under an end pin. And before declaring a feature dead, isolate WHY it fails with a direct probe (routeToTarget on a hand-built minimal target succeeded in 2s — that single experiment overturned "the planner can't do it"). L20's beam-first advice is still the fast path for mildly-scattered types; the planner is the fallback the tool now takes automatically.
- Evidence: scratchpad water-gather.js/water-gather-pin.js (bare 5-line routes 5/5 in ~2s, 8/8 placements with end pin), debug-planner.js (found the clearAllComboFloor=5 bug), planner-water.js + CLI dry-run (5/5, 3 combos, end 5,0). verify.js CA1-CA3.
- Affected files: algorithm.js TargetPlanner.linePlacements/partitionCount/coverageSets/planClearAllTargets/solveClearAll + clearAllComboFloor + solve() dispatch; phone/autospin.js clear-all escalation + abort hint; PROJECT-FACTS P14; verify.js CA1-CA3.

## L23 2026-07-08 Git Bash mangles `/sdcard/...` adb arguments into Windows paths — use PowerShell (or Node execSync) for adb shell commands
- Symptom: `adb shell screencap -p /sdcard/x.png` run via the Bash tool printed screencap's usage/help and exited 1, as if the flag parsing broke.
- Root cause: MSYS/Git Bash path conversion rewrites POSIX-looking arguments (`/sdcard/x.png` → `C:/Program Files/Git/sdcard/x.png`) before adb sees them; the embedded space splits it into extra args on the device side. The same command works from PowerShell and from Node's execSync (phone/autospin.js) because neither converts paths.
- Lesson: on this machine, run adb commands with device-side paths from PowerShell, not the Bash tool (or prefix `MSYS_NO_PATHCONV=1` if Bash is unavoidable). Companion to L7 (never redirect adb binary stdout through PowerShell `>`): between the two, the safe recipe is PowerShell + `adb pull`, exactly what autospin.js's captureRaw does.
- Evidence: identical screencap command failed under Bash tool, succeeded under PowerShell, 2026-07-08.
- Affected files: none (tooling pitfall only).

## L24 2026-07-09 Solver speed: profile first — GC/allocation was HALF the cost; "obvious" fixes bought nothing
- Context: goal was <5s solves (from 30-150s) via worker_threads parallelism. Parallelism alone gave only 6.5x (149.8s→23.2s) on 8 physical cores — the rest came from profiling.
- What the profile said vs what intuition said: `node --prof` showed GC = 46% of solver CPU and findComboGroups at 8.8µs for a trivial 30-cell no-match scan (Sets, nested bool arrays, closures, per-cell tuple literals). Meanwhile the "obvious" dedup-before-scoring fix bought ~0ms (duplicate board+cell children were rare — MEASURE the duplicate rate before building around it: a cross-step board-key score cache hit 39% where same-step dedup hit ~0%).
- Fixes that worked (all bit-exact, guarded by fixed-board output diffs + verify.js): typed-array/bitmask scan internals + module-level scratch buffers (8.8→1.0µs), cross-step score cache (weight/steer/qualification are functions of the board alone), compact fromCharCode keys instead of grid.join(';'), worker resourceLimits young-gen sizing (+15%). Net: 149.8s→5.6s (27x), sequential alone 3x.
- Pitfalls hit on the way: (1) editing algorithm.js WHILE a background benchmark loop was still running made the loop require() a half-edited file — SyntaxError mid-sweep and garbage results; never edit source a queued background node job will load, run benchmarks from an immutable copy or wait. (2) `new Worker(..., {execArgv: ['--max-semi-space-size=64']})` throws "invalid execArgv flags" — V8 flags don't go in worker execArgv; use `resourceLimits.maxYoungGenerationSizeMb`. (3) Beam SHARDING is a quality lottery for rare qualified states (clear-all): --workers 10 missed 6/6 where 8/12/15 hit it — per-shard width is the driver (P18); don't shrink total beam just because wall time dropped.
- Evidence: prof summary (GC 21.6% total / 46% nonlib), micro-benches in session scratchpad, bench-board.txt = the User's 151s board; before/after CLI runs in P18.
- Affected files: algorithm.js (findComboGroups, DoraSolver.solve cache/keys, _nd precompute, emitFrontier/seedBeam hooks), phone/parallel.js (new), phone/autospin.js (--workers wiring, planner beam cap), verify.js (PW1-PW4), PROJECT-FACTS P18.

## L25 2026-07-09 An all-or-nothing "qualifies" gate silently dropped a MANDATORY demand when combined with an unreachable OPTIONAL one
- Symptom (live User report): `--clear-all water --first-combos max` returned `CLEAR_ALL=Water:0/5 MISS` after 328s, even though the SAME board solved plain `--clear-all water` (no first-combos) fine: 5/5 water, 7 combos, 5.9s.
- Root cause: `DoraSolver.solve()`'s final pick was `bestQualified ?? best`. `qualifies` ANDs together the OPTIONAL minFirstCombos/minFirstRunes target with the MANDATORY clearTypes demand (P14) into one flag. `max` mode seeds `minFirstCombos = bound` (an aggressive, sometimes-unreachable-jointly-with-clear-all target) — when unreachable, `bestQualified` is null and the pick fell straight to `best`, the fully UNCONSTRAINED highest-weight solution, discarding the mandatory demand even though a clear-all-satisfying candidate existed in the very same beam (confirmed by direct instrumentation: `best` cleared 0/5 water at 8 combos, a same-beam candidate cleared 5/5 at 7 combos).
- Fix: added a third tier `bestClearAllOnly` (best solution meeting ONLY the mandatory demand) tracked alongside `best`/`bestQualified`; pick = `bestQualified ?? bestClearAllOnly ?? best`. Threaded through phone/parallel.js's solveDoraParallel merge (prefix + all worker shards) with the same three-tier priority. Also parallelized the `max`-mode path itself (`solveMaxFirstCombosParallel`, new) since the bug investigation surfaced it was fully sequential — 328s → 18s on the repro board (12s at a smaller beam, same quality).
- Lesson: whenever a MANDATORY constraint (must always hold) is combined with an OPTIONAL steering target (nice-to-have, best-effort) in one search, the "did we meet everything" gate must not be the only fallback tier — a single AND'd qualification flag will silently let an unreachable optional target drag down a satisfiable mandatory one. Test this shape deliberately: pass an OPTIONAL target that is CERTAINLY unreachable (e.g. minFirstCombos=20 on a 30-cell board) and confirm the mandatory demand still holds in the result (verify.js MD1-MD3) — don't just test the happy path where both targets are jointly reachable.
- Evidence: live CLI repro before/after on the User's exact reported board and command; node -e instrumentation showing best vs bestClearAllOnly diverge with bestQualified===null; verify.js MD1-MD3 (131/131 total).
- Affected files: algorithm.js (DoraSolver bestClearAllOnly tier + pick priority, computeMaxFirstCombosBound extraction), phone/parallel.js (solveDoraParallel merge, new solveMaxFirstCombosParallel), phone/autospin.js (--first-combos max now calls solveMaxFirstCombosParallel, planner beam cap applied there too), verify.js (MD1-MD3), PROJECT-FACTS P19.

## L26 2026-07-09 A flat beam cap is wrong in BOTH directions — escalate instead of picking one fixed budget
- Symptom (live User report, felt like "the algo is stuck"): `--start 4,1 --end 0,4 --clear-all heart` plateaued at EXACTLY 4/5 hearts across beam 16000, 32000, 64000, and 128000 (the last took 10 minutes) — a hard wall that widening the outer --beam never crossed.
- Root cause: the previous session (L25/P19) fixed a different bug by capping the coverage planner's OWN routing beam at a flat 2000, reasoning that L22's single/no-pin cases route fine at 300 and a bigger cap just wastes time on doomed escalations. That reasoning didn't generalize: a HARD dual start+end pin (both ends fixed, unlike L22's end-only case) needs FAR more routing width. Isolated by testing each pin alone (all solved 5/5 in <2.3s) vs both together (MISS) — confirmed beam-dependence directly: routing at 300/2000/4000 all `routing-failed`, 8000 succeeded (151s sequential / 48s parallel), 16000 also succeeded but SLOWER (108s — once wide enough, MORE width just makes each target costlier, it doesn't help). So: 2000 is too low for hard-dual-pin (silent MISS, this bug); a naive fix of "just raise the cap" would be too high for easy cases (wastes time, the ORIGINAL P18 complaint that motivated the cap).
- Fix: stopped picking a single fixed number entirely. `solveClearAllParallel` now plans coverage targets once (proven beam-INdependent — pure board geometry) then ESCALATES routing width (300→2000→8000→20000→ceiling), returning at first success. Easy cases still resolve at step 1 (fast); hard-but-solvable cases keep escalating until they succeed; genuinely impossible cases short-circuit via the beam-independent `no-feasible-target` check instead of burning time at every step.
- Lesson: whenever a search parameter's needed value varies by orders of magnitude across inputs (here: single-pin vs dual-pin routing difficulty), do NOT hardcode one budget from a single motivating case, whichever direction you tune it. Escalate from cheap to a real ceiling instead, and cleanly separate "beam-independent structural fact" (compute once, short-circuits every retry) from "beam-dependent search result" (the only thing that needs re-trying at wider widths). This is the same shape as L20's "raise beam, don't shrink it" but sharper: L20 said which direction to move a knob; this is about not having ONE knob value be safe for wildly different problem difficulties.
- Evidence: live CLI repro (5 escalating manual --beam retries by the User, all landing on 4/5 hearts); direct TargetPlanner.solveClearAll and solveClearAllParallel beam sweep isolating the exact threshold (300/2000/4000 fail, 8000+ succeed); before/after on the User's exact command (stuck at 4/5 for minutes → 5/5 in 66s); regression check that the earlier P18 single-pin fast case (4.9s) is unaffected.
- Affected files: phone/parallel.js (solveClearAllParallel rewritten to escalate; plans targets once, routes at increasing widths), phone/autospin.js (both call sites no longer pre-cap the planner beam — pass the real --beam-derived ceiling through), PROJECT-FACTS P18/P19 (superseded notes) + P20 (this fix).

## L27 2026-07-10 Card badges use a DIFFERENT color palette than board runes, AND the same element can render at multiple brightness/saturation levels
- Context: implementing --drag-from (drag from one of 6 character cards to inject a rune of that card's element onto the board). Calibrated a CARD_SIGNATURES table from a live screenshot, separate from the board's existing SIGNATURES.
- Finding 1: a card badge's Dark sample measured ~115 units from the board's own Dark signature (180,33,208) — far past the board's MAX_SIG_DIST=60. The UI badge icon palette is NOT the same as in-board rune colors; reusing SIGNATURES for card badges would have silently misclassified everything. Always calibrate a NEW signature table per distinct UI surface, never assume "same element = same measured color" across different rendering contexts (badges vs board cells vs enhanced/electric variants — this project already knew the latter, this extends the principle to an entirely different UI element).
- Finding 2: three cards visually identified as "the same character, same element" (Dark) measured THREE notably different colors (219,141,211), (192,110,187), (192,76,202) — internal spread up to ~71 units, more than the board's whole MAX_SIG_DIST. User confirmed all three are genuinely Dark. This game renders a card's element badge with real per-instance brightness/saturation variance (unclear why — possibly per-character art style, awakening state, or lighting), unlike board runes which use one fixed color per element. Handled the same way the project already handles enhanced/frozen/electric board-rune variants: multiple signature entries mapping to the same type, with the classification threshold (MAX_CARD_SIG_DIST=90) set above the observed internal spread but well below the cross-type distance (200+).
- Finding 3 (real-device pitfall): a card can be LOCKED (already used this turn/on cooldown), shown as a metallic chain-link overlay that completely replaces the badge. First live test hit this by chance (card 1 was locked) and the unknown-guard correctly refused (dist=99, past threshold) instead of misclassifying — validated the guard against a real, previously-unanticipated UI state on the very first live run. Confirms L8's "unknown-guard refuses rather than guesses" design holds up against unanticipated overlays, not just anticipated variants.
- Lesson: when adding recognition for any NEW UI surface (not just new element variants on an already-covered surface), (1) never assume an existing signature table transfers, (2) expect the same element to have real measured variance across instances and calibrate multiple reference points rather than one average, (3) trust the unknown-guard to catch surprising states you didn't anticipate (like a lock overlay) — that's exactly what it's for, don't be tempted to lower the threshold just to make a first test pass.
- Evidence: live screenshot analysis (Python/PIL pixel sampling with coordinate-grid overlays) for calibration; live end-to-end device test showing the correct ABORT on a locked card and correct recognition+injection+solve on two unlocked Wood cards.
- Affected files: phone/autospin.js (CARD_SIGNATURES, CARD_BADGE_X/Y, CARD_TOUCH_Y, readCardBadge, cardDragPrefix, --drag-from wiring); PROJECT-FACTS P21.

## L28 2026-07-10 A color-under-overlay recognition problem needs brightness-invariant (chromaticity) matching, not raw RGB distance
- Symptom: the edge-hazard fiery overlay's hidden base classification (`classifyEdgeHazardBase`) gave uniformly WRONG/collapsed results (all 5 cells in a column read as "Heart") on a live `--check` run, despite a manual pixel inspection minutes earlier clearly showing distinct colors peeking through the flame per cell (reddish/pink/purple).
- Root cause: raw Euclidean RGB distance against `EDGE_HAZARD_SIGNATURES` is sensitive to overall BRIGHTNESS, which varies between captures (scene lighting, flame animation phase — measured ~13% swing in absolute RGB magnitude between two captures of the SAME board). Near-boundary cases (e.g. Fire vs Heart, both warm-toned) flip classification under a brightness shift even though the true element never changed. Confirmed by computing g/r and b/r ratios (chromaticity) for both captures: the ratios were essentially IDENTICAL across the ~13% brightness swing, while raw RGB distance gave different nearest-signature results.
- Fix: classify in chromaticity space (r,g,b each normalized by their sum) instead of raw RGB — exactly the same principle already used elsewhere in this codebase for `electricBase` (P11/L16: strip the glare floor, read residual hue). Recalibrated the distance threshold to the new scale (chromaticity distances are ~0.01-0.05, not ~10-100 like raw RGB).
- Lesson: whenever classifying a color that sits UNDER or NEXT TO a variable-brightness overlay (flame, glow, lighting, animation), do not assume raw-RGB nearest-signature matching will be stable across captures just because it worked once. Test explicitly across brightness variation (two captures of the same known state, at different times/lighting) before trusting a color classifier — a single successful calibration run does not prove brightness-invariance. When in doubt, normalize (chromaticity, or a glare-floor-subtraction like electricBase) rather than compare absolute values.
- Evidence: two live screenshots of the same board state (rgb magnitudes ~13% apart) — raw-RGB classification flipped row 0's result between them; chromaticity classification gave identical, correct results (matching the User-confirmed Fire/Heart/Dark pattern) in both.
- Affected files: phone/autospin.js (`chroma()` helper, `EDGE_HAZARD_SIGNATURES` precomputed chroma field, `classifyEdgeHazardBase` rewritten, `MAX_EDGE_HAZARD_DIST` rescaled to chromaticity units, new `cellUnknown(c)` helper unifying the per-cell-type threshold check across 4 call sites); PROJECT-FACTS P22.

## L29 2026-07-10 When a color-classification approach keeps failing (raw RGB, then chromaticity too), stop guessing and ask for ground truth instead of trying a third heuristic
- Symptom: extending the P22 chromaticity fix (which correctly solved the fire-overlay's brightness-sensitivity problem) to a DIFFERENT overlay — a translucent prohibition-ring tint locking one element board-wide — didn't work. Raw-RGB nearest-match landed on the right answer (Wood) but at distance ~98 (past the confidence threshold); chromaticity nearest-match, which fixed the OTHER overlay, ambiguously flipped to a WRONG answer (Light) for two cells confirmed to be the same locked element.
- Root cause: the two overlays are structurally different. The fire overlay left a clean, narrow GAP where pure base color showed through untouched — chromaticity cleanly recovered it because the sample itself was uncontaminated, just varying in brightness. This ring overlay has no such clean gap: its white outline and diagonal slash lines dilute the ENTIRE whole-cell average pervasively, so no per-pixel-region trick or brightness-normalization salvages a clean signal — the sample itself is contaminated, not just brightness-shifted.
- Lesson: don't assume a fix that worked for one visual-overlay problem generalizes to a superficially-similar one. Test it directly (I did: measured raw-RGB AND chromaticity distance for the same known-Wood cells, both gave poor/wrong results) before trusting it. More importantly: when a mechanic's underlying property is a SINGLE STAGE-WIDE CONSTANT (not per-cell varying — all 4 ringed cells are the same locked element), the reliable source of truth is asking the user (who can read the boss's own skill text) once, not attempting per-cell pixel recognition at all. Recognize when a problem has moved from "needs a better pixel heuristic" to "needs an explicit, user-confirmed, overridable input" — the latter is both more reliable AND less code.
- Evidence: live pixel measurements on 4 confirmed-Wood ringed cells — raw RGB dist~98 (nearest Wood, but past MAX_SIG_DIST=60); chromaticity distance 0.23-0.24 to nearest (Light, wrong) vs true Wood chromaticity distance 0.37 (worse) — both approaches unreliable; a live repeat `--check` run independently showed 2 of 4 cells flip-flopping between "Wood%" and "Light%" using the (reverted) chromaticity approach, confirming genuine instability, not a one-off misread.
- Affected files: phone/autospin.js (`noSolvableTypeOverride()` + `--no-solvable-type` CLI flag, replacing the pixel-guess with an explicit User-confirmed default); PROJECT-FACTS P22 (updated to document both the fire-overlay chromaticity fix that worked and this ring-overlay case where it didn't).

## L30 2026-07-10 Removing a positional gate from one overlay detector can make it collide with an unrelated overlay that shares the gated positions
- Symptom: after correctly generalizing the fire-hazard detector to scan the whole board (not just columns 0/5, per User correction), 3 specific cells that were BOTH fire-hazard AND Wood-locked-ring got misclassified — the fire branch's `classifyEdgeHazardBase` unconditionally overwrote the type with a wrong guess (Light), because its signature table has no Wood entry at all.
- Root cause: the two overlays' detectors were both written and tested in isolation, each assuming it was the ONLY thing that could be true for a given cell. Column 0/5 happened to be exactly where the User later found a real co-occurrence (a cell simultaneously fire-hazard AND ring-locked) — once the fire check stopped being restricted to those columns, it started running (and clobbering) on cells the ring-detector had already correctly claimed, or SHOULD have claimed but couldn't (see next point).
- Second layer of the same bug: the no-solvable ring detector ALSO failed on these same dual cells, but for a different reason — its wide-area green-PERCENTAGE threshold (calibrated for an unobscured ring) never crosses when flame covers most of the cell (measured live: ~0.012 vs its 0.18 requirement). A percentage-based detector calibrated against one visual condition (ring alone) silently breaks under a second overlapping condition (ring + flame) that dilutes the signal below threshold — it doesn't error, it just quietly returns false.
- Lesson: whenever two independent per-cell overlay/status detectors exist in the same recognition pipeline, explicitly test whether they can BOTH be true for the same cell before assuming "detector A fired" implies "detector B's effect is absent." Don't let a later detector's classification unconditionally overwrite an earlier one's — order checks so the more reliable/structural one (here: a stage-wide constant, immune to the other overlay) wins, and add a lower-threshold secondary check specifically for the "signal diluted by an overlapping effect" case rather than assuming the primary detector's calibration transfers to combined conditions.
- Evidence: live pixel measurement across 3 confirmed dual-hazard cells vs a genuine fire-only cell vs a genuine unobscured-ring cell, isolating the green-percentage gap (0.042-0.047 dual vs 0.005 fire-only vs 0.55 unobscured) that a secondary, lower threshold (0.02) cleanly separates; before/after `--check` output on the live board confirming both markers (`Wood%$`) now appear correctly with zero false positives on the other 7 fire-only edge cells on the same board.
- Affected files: phone/autospin.js (`hasGreenTintUnderFire` new secondary detector; reordered the no-solvable / edge-overlay checks so no-solvable-or-green-tint wins over the edge classifier's clobber); PROJECT-FACTS P22.

## L31 2026-07-10 A misleading diagnostic display (wrong decimal precision for the value's actual scale) hid the real bug behind a confusing symptom
- Symptom: "board did not settle within 30s" with `SETTLE_UNKNOWN=(5,3)d=0rgb=(...);(0,4)d=0rgb=(...)`. The `d=0` looked like these cells were CONFIDENTLY classified (matching the convention elsewhere in this codebase where dist=0 means "known, suppressed") — a real red herring that pointed away from the actual problem.
- Root cause: edge-hazard cells are classified in CHROMATICITY space (distances ~0.01-0.1, per L28/P22), but the diagnostic log used `.toFixed(0)` — zero decimal places. Any chromaticity distance under 0.5 rounds to "0" on display, so a GENUINE confidence failure (dist=0.12, correctly flagged unknown because the true type — Water — had no calibrated signature at all) displayed identically to a deliberately-suppressed dist=0. The bug wasn't a classification bug at that point — it was a display-precision bug that made a correct "unknown" result look like a broken "known but flagged anyway" result, wasting investigation time on the wrong hypothesis.
- Separately (found while investigating the real cause): the overlay DETECTOR itself (not just the type calibration) had a knife-edge threshold — `darkPct>0.28` — that all 10 genuine hazard cells on a live board measured within 0.272-0.303, meaning ~30% of them randomly fell on the wrong side of the line depending on capture-moment noise. A threshold this close to the actual measured cluster of true positives will flip-flop; always leave real margin, and validate that margin against BOTH the true-positive cluster AND the closest known false-positive candidate (here: a plain Light gem at 0.173) before trusting a threshold.
- Lesson: when a diagnostic value is displayed with fewer decimal places than its actual scale requires, it can silently corrupt debugging — always match display precision to the value's real magnitude (a chromaticity-space number needs more decimals than a raw-RGB-space number), especially when the same display function/format string is reused across two differently-scaled metrics. And when a percentage/ratio threshold is this close to the measured cluster of genuine positives, that's a sign that it needs a full-board validation sweep (measure ALL cells, not just the ones that happened to work in the first test) before shipping it as the boundary.
- Evidence: full-board `darkPct` scan (all 30 cells of a live board) showing the 0.272-0.303 true-positive cluster sitting just above the old 0.28 cutoff, and a false-positive candidate at 0.173 validating the new 0.20 threshold's margin on both sides; before/after settle-loop test on the exact board that previously timed out, now settling and solving immediately.
- Affected files: phone/autospin.js (`hasEdgeNoClearOverlay` threshold 0.28→0.20; `EDGE_HAZARD_SIGNATURES` Water entry added; new `distStr(c)` helper replacing 3 raw `.toFixed(0)` call sites for cell-distance display); PROJECT-FACTS P22.

## L32 2026-07-10 A "burst reclassification" pass silently discarded ALL overlay detection for EVERY cell the moment ANY cell anywhere looked like an electric candidate — root-caused via file-based (not console) debug logging after stdout/stderr interleaving gave misleading traces
- Symptom: after fixing the settle-timeout and threshold issues, edge-hazard cells STILL showed "Fire*" (plain classify() + thorn) instead of "X$" (edge-hazard + correct type) — even though direct instrumentation of `hasEdgeNoClearOverlay` proved it was returning `true` and `c.edgeNoClear=true` WAS being set, moments before the cell got printed as if none of that had happened.
- Root cause: `readBoardFromScreen` has a SECOND pass, gated on `anyCandidate` (true if ANY cell anywhere triggered a preliminary electric-glow check) — a multi-frame burst capture to confirm/reject electric shocks. Its "not electric" branch called `classify(stats, true)` directly on raw stats and overwrote `cells[gy][gx]` with a BRAND NEW object that had never been through the no-solvable-ring or edge-fire-hazard layers at all. So whenever ANY cell (even one unrelated to the fire/ring overlays, anywhere on the 30-cell board) looked like a momentary electric-glow candidate, this pass silently threw away every overlay classification for EVERY cell on the board and replaced it with a plain signature match. The two passes were maintained as separate, duplicated classification logic that quietly drifted out of sync as overlay layers got added to one but not the other.
- Debugging pitfall along the way: my first debug attempts used `console.error`, piped through `2>&1 | Select-String`. The printed order looked contradictory (a "set edgeNoClear=true" line immediately followed by a read showing edgeNoClear absent) — this LOOKED like impossible/racy behavior and wasted real time chasing async/caching theories. Switching to `fs.appendFileSync` (a single guaranteed-ordered stream, no stdout/stderr merge-order ambiguity) immediately confirmed the trace was real and consistent, just pointing at a different bug (two separate classification code paths, not a race). Lesson: when a live trace seems to show something FLATLY IMPOSSIBLE (a value visibly set then immediately absent with no code in between that could unset it), suspect the LOGGING mechanism itself (mixed streams, buffering, interleaving) before inventing exotic explanations for the target code — verify causality with a single ordered sink first.
- Structural lesson: when the same per-cell classification decision can be reached via two different code paths (a first pass and a later reclassification pass), duplicating the logic across both is a standing invitation for exactly this kind of silent divergence as new layers get added to one but not the other. Fixed by extracting a single `classifyBoardCell(img, gx, gy, noSolvableType, skipGlow)` helper containing the FULL layered classification (base signature -> go-overlay -> no-solvable ring -> edge fire hazard), called identically by both passes — the burst pass now picks the least-white FRAME (not just its stats) and routes it through the same helper, so both passes can never drift apart again.
- Evidence: `fs.appendFileSync`-based trace showing the exact push-then-immediately-different-read sequence, isolated to the burst reclassification loop by code reading; live board that previously plateaued at "Fire*" everywhere now correctly resolves all 10 edge cells with proper types and settles/solves immediately.
- Affected files: phone/autospin.js (new `classifyBoardCell` helper; both `readBoardFromScreen`'s first pass and its electric-burst reclassification now call it); PROJECT-FACTS P22.

## L33 2026-07-10 A verified-safe PLAN can still fail at EXECUTION if the physical drag doesn't faithfully reproduce it — and "must not touch" vs "must not dissolve" are different constraints needing different responses
- Symptom: User watched a fire-hazard cell's rune actually dissolve during a real spin (including via cascading/gravity), even though the exact board+path was independently reconstructed via `BoardSimulator.resolve` and PROVEN to never dissolve any hazard-column cell across all 6 cascade waves of that solve.
- Root cause: the PLAN was correct; the DEVICE EXECUTION of the drag apparently wasn't guaranteed to match it. The held rune trails the physical finger and can cut a diagonal corner through a cell the path never actually visits at speed — already a known, handled effect for electric cells (P11), where cutting a corner into one is instantly fatal to the whole drag. The same physical trailing-rune effect can just as easily make the ACTUAL final board arrangement differ from the VERIFIED-safe planned one near a fire-hazard cell, even though fire-hazard cells don't forbid touching at all.
- Important distinction (User-clarified mid-fix, correcting my first framing): electric cells forbid touching/passing through AT ALL (instant interrupt) — the fix there is "avoid entering the cell." Fire-hazard cells allow touching/dragging through completely freely — the ONLY constraint is the rune there must not end up dissolved. These are different constraints, but the SAME underlying physical effect (corner-cutting trailing rune) can break both, so the same MECHANISM (extra dwell time at turns near the sensitive cell, letting the trailing rune settle before changing direction) fixes both — just for different reasons. Almost committed a fix framed around "avoid touching hazard cells," which would have been solving the wrong problem (touching was never the issue) — caught by asking the user to confirm before continuing down that path.
- Lesson: when a live-observed failure contradicts a verified-correct plan, don't immediately suspect the planning algorithm — check whether the EXECUTION LAYER (physical touch/drag simulation) can diverge from the plan under real-world timing, especially for effects already known to matter at speed (corner-cutting). And when reusing an existing safety mechanism (dwell-near-electric) for a new, superficially-similar-but-distinct hazard, verify the ACTUAL constraint semantics with the user rather than assuming the new hazard behaves like the old one just because both are described as "don't let this happen" — get the reasoning right before writing the fix, not just the mechanism.
- Evidence: standalone reconstruction of the dwell logic against the User's exact real path, confirming dwell=8 fires at every turn adjacent to a hazard-column cell after the fix; the underlying board+path was already proven safe at the planning level via direct BoardSimulator reconstruction (see P22's earlier verification), isolating this as a pure execution-fidelity gap.
- Affected files: phone/autospin.js (`gridPathToScreenPath`'s cell-adjacency param renamed `dwellCells`, generalized doc comment; new `hazardDwellCells` built in `main()` from `c.edgeNoClear || c.noSolvable`; call sites pass the union of priority + hazard cells); PROJECT-FACTS P22.

## L34 2026-07-10 A "never dissolve" constraint modeled as structural exclusion silently "rescued" boards the real game would NOT have rescued
- Symptom: User watched a fire-hazard cell's rune actually dissolve on a real spin, even after the drag-execution dwell fix (L33). Live evidence: `BOARD_AFTER_ROW2=Water$,Water,Water,Water,Dark,Heart$` showed 4 contiguous Waters including the hazard cell at (0,2), and the tool's own `FIRST_WAVE_CLEARED` list only showed the OTHER 3 cells — proving the algorithm itself was trimming the hazard cell out of the run, not that execution drifted from the plan.
- Root cause: `CELL_FLAGS.NO_DISSOLVE` (reused from the sealed/fence mechanic, P6/P9) structurally EXCLUDES a cell from ever joining a match — it breaks a run into pieces on either side of the excluded cell. That's the CORRECT model for sealed columns (which really do work that way), but WRONG for the fire hazard: the User confirmed the real game does not break the run at the hazard cell — it sweeps the WHOLE contiguous run (hazard cell included) into one dissolve, exactly like normal match-3 rules, and that dissolve is the bad outcome to avoid. The NO_DISSOLVE model was silently "rescuing" a would-be 4-match into a smaller 3-match that the solver read as a perfectly safe, even good, outcome — while the real engine would have swept the hazard cell in too.
- Lesson: when two mechanics produce the same USER-FACING description ("this cell must never dissolve"), don't assume they share the same underlying MODEL. Ask specifically: does the constraint work by making the bad state impossible to construct (structural exclusion — breaks runs, like sealed columns), or by making an otherwise-normal state simply forbidden as an answer (post-hoc rejection — the state CAN form, using completely normal rules, but must never be the final choice)? Modeling the second kind as the first kind doesn't just under-protect — it can ACTIVELY hide the danger by making the solver believe a bad arrangement is fine, because the model itself never lets the danger fully materialize in simulation.
- The fix: match runs NORMALLY (no exclusion at all), then check whether any resulting group (in ANY cascade wave, not just first) touches a hazard position, and reject the whole candidate board as an answer if so — the same "structural exclusion vs. post-hoc rejection" pattern already used correctly elsewhere in this codebase for `clearTypes`/`firstWaveNoTypes` (both post-hoc rejection), just not previously applied to this specific mechanic.
- A related pitfall while implementing: one call site (`solveMaxFirstCombosParallel`) explicitly reconstructs its options object field-by-field rather than spreading the original — this silently drops any NEW option key added later (like `hazardPositions`) unless every such site is remembered and updated. Prefer spreading over explicit reconstruction when forwarding options through multiple layers, specifically to avoid this class of silent gap.
- Evidence: verify.js HZ1 (a normal 3-run genuinely still includes the "hazard" cell — not trimmed) proves the model matches reality; HZ4 proves DoraSolver never returns a violating board even when the natural/default arrangement would violate it; live re-test of the User's exact reported board shows `FIRST_WAVE_CLEARED` now correctly excluding all hazard positions.

## L35 2026-07-10 Fire-hazard overlay (P22) can coincide with the sealed-column thorn overlay (P6) on the SAME cells, and the fire-detection code was silently discarding the thorn read whenever this happened
- Symptom: User reported a live board where cols 0/5 (all rows) are BOTH fire-hazard AND thorn-sealed, but `--check` printed `SEALED_COLS=[]` — the sealed-column mechanic went completely undetected.
- Root cause: `classifyBoardCell` unconditionally sets `c.thorn = false` the moment `hasEdgeNoClearOverlay` fires (written when the two overlays were assumed mutually exclusive — true at the time, disproven by this board). Since sealed-column auto-inference (`inferSealedColumns`) counts per-cell `c.thorn` flags (>=3 per column), forcing it false on every hazard cell silently zeroes out the whole column's count whenever fire-hazard cells happen to sit in a sealed column.
- Investigated a pixel-level fix first (live diagnostic dump of whole-cell/narrow-patch darkPct across all 30 cells): all 10 edge/hazard cells clustered at darkPct 0.21-0.23 vs 0.05-0.09 elsewhere — a big, clean-looking margin — but this board has NO fire-only (non-thorn) comparison cell to isolate whether that elevated darkness comes from thorn, from the flame's own shadow, or both; P22's earlier measurements suggest fire alone can already push darkPct into a similar range using a different sampling function. Per protocol step 4, did NOT force a threshold from this ambiguous, unpaired sample.
- Fix: none needed in code — asked the User directly whether the sealed-column card was active this battle (P6's mechanic is a stage-wide, User-controlled constant, not a per-cell visual property to re-derive under a second overlay). Confirmed yes; the existing `--sealed 0,5` CLI override (already built for exactly this "auto-detection can't see it, so state the known fact directly" situation, same pattern as `--no-solvable-type`) fixes it for the session with zero risk of a wrong guess.
- Lesson: when two independently-documented overlays can appear on the same cell, don't assume old code that special-cased their earlier apparent mutual exclusivity is still safe — audit it (protocol step 7) the moment a User report suggests co-occurrence. And when a property is stage-wide/session-level (User activated it, doesn't vary cell-to-cell) rather than a per-pixel classification problem, prefer asking + an explicit CLI override over inventing a new visual heuristic without a clean calibration sample — this project already has the override mechanism, it just wasn't reached for.
- Affected files: phone/autospin.js (`classifyBoardCell` line ~624-626, `hasEdgeNoClearOverlay` branch — the unconditional `c.thorn = false` is now known-unsafe when the cell is genuinely in a sealed column; not yet rewritten, `--sealed` override is the current mitigation); PROJECT-FACTS P6, P22.

## L36 2026-07-10 Almost modeled a new "never dissolve" mechanic (shield) as POSITIONAL when it's actually PER-RUNE — caught by asking before shipping, not after
- Symptom: none live yet — caught during implementation, before delivery. User reported a new Wood-rune overlay: "cannot dissolve even match >=3, can drag, can pass through." That phrasing alone is ambiguous between two totally different models already used elsewhere in this codebase (protocol 06 step 2's exact question: positional vs per-rune).
- Root cause of the near-miss: sealed/fence columns (P6/P9) use the SAME user-facing phrasing ("cannot dissolve, can drag through") and are modeled via positional `CELL_FLAGS.NO_DISSOLVE` — that model was reached for FIRST, by pattern-matching the description to the most recently-worked-on similar-sounding mechanic, without checking whether THIS one binds to the cell or the rune. Wired it, tested it with a `--dry` solve, got a plausible-looking result (the solver avoided matching the shield cells) — a passing test that would NOT have caught the bug, since a passing first-wave solve looks identical whether the constraint is bound correctly or not; the divergence only shows up once the rune actually MOVES away from its starting cell.
- Lesson: "cannot dissolve, can drag/pass through" is a description of BEHAVIOR, not a MODEL. Two mechanics can share that exact description and need opposite implementations (`CELL_FLAGS` positional vs a per-rune board value like `FROZEN`) depending on one fact not present in the description: does the constraint stay at the grid position, or follow the specific rune when it moves? This is precisely protocol 06 step 2's first question — asked it explicitly ("does the shield travel with the rune, or stay at the position") before declaring the feature done, and the answer (travels with the rune) reversed the whole implementation approach. A `--dry` solve alone would NOT have surfaced this — the two models only diverge once a shielded rune is actually dragged off its origin cell, which a single solve's first-wave-only inspection doesn't necessarily exercise or highlight.
- The fix: once "travels with the rune" was confirmed, recognized that the REQUIRED behavior (never dissolve, moves/drags/falls freely, follows the rune) is mechanically IDENTICAL to the existing FROZEN ice mechanic (P16) — reused that machinery directly (`board.fromArray` maps `c.shield` to `FROZEN` alongside `c.frozen`) instead of adding new solver semantics, since this project already has exactly the right primitive.
- Evidence: `--dry` re-run after the fix shows `BOARD_AFTER_ROW` reporting `Frozen` at the shielded runes' NEW post-drag positions (not their original cells), confirming the per-rune binding; before the fix this couldn't have been distinguished from the (wrong) positional model without a specific check.
- Affected files: phone/autospin.js (removed the `CELL_FLAGS.NO_DISSOLVE` shield branch in the `flagGrid` builder; `board.fromArray` now maps `c.frozen || c.shield` to `FROZEN`); PROJECT-FACTS P24.

## L37 2026-07-10 Two "2 rounds left" FROZEN calibration entries (P16) turned out to be mislabeled plain thorn cells, silently colliding with unrelated confirmed types
- Symptom: User reported 6 live cells reading `Fire#*` (frozen) that were actually plain Water/Heart under a thorn overlay, with NO frost mechanic active anywhere on the board (User-confirmed).
- Root cause: `classify()`'s nearest-signature search scans ALL of `SIGNATURES` by raw-RGB distance, independent of the cell's own separately-detected thorn boolean — so even a `thorn:false` frozen entry is reachable for a thorn-darkened cell. Direct distance math against the User's confirmed ground truth showed two frozen entries (`{thorn:true,rgb:[114,112,130]}` from 2026-07-09, `{thorn:false,rgb:[178,112,123]}` from 2026-07-10) sitting CLOSER to plain thorn-Water and plain thorn-Heart than those types' own correct signatures. Checked chromaticity as a possible fix (per the P22 pattern) — it did NOT help, and made one case worse. Colliding with two unrelated, independently-confirmed types is strong evidence the original samples were mislabeled at capture time (probably an ordinary thorn cell visually mistaken for a frozen shell), not a real third category — the underlying mistake is the same class as L2/L8 (guessing a calibration from visual impression rather than confirmed ground truth), just discovered later, via collision, rather than at capture time.
- User pushback on the first fix attempt: initially planned to just delete the two bad entries. User asked for a less-destructive alternative. Landed on a `disabled:true` field checked in `classify()`'s matching loop — keeps the historical rgb/comment (useful if a genuine sighting recurs and needs comparing against what was previously assumed) while fully removing it from the active candidate pool, and re-enabling is a one-line flag removal instead of re-deriving the value from scratch.
- Lesson: when two calibrated signatures for DIFFERENT stated conditions (frozen vs plain thorn) turn out to collide against live, independently-confirmed ground truth, don't assume the newer/more-specific-sounding one is right by default — check which one is actually consistent with confirmed reality, and be willing to conclude an OLDER "confirmed" entry was never properly confirmed at all. Also: when retracting a signature, prefer a soft-disable flag over deletion — it costs nothing at runtime (one `continue` check) and preserves the exact data needed to compare against the next real sighting.
- Evidence: full-board `--check` re-run after the fix shows all 6 previously-misread cells now correctly reading `Water*`/`Heart*`, matching the User's stated ground truth exactly, on the same live board/capture.
- Affected files: phone/autospin.js (`SIGNATURES` — two entries flagged `disabled:true` with a detailed comment; `classify()`'s matching loop now skips `disabled` entries); PROJECT-FACTS P25.

## L38 2026-07-10 Fixing a signature collision by adding a second calibration point can regress OTHER, already-correct cells — test the fix against everything nearby, not just the reported cell
- Symptom: User reported `(5,3)` (confirmed Dark) reading as Heart. First fix (following the L37 pattern of trusting fresh live ground truth) added the live-measured value as a second Dark+thorn signature point, alongside the existing one. Re-running `--check` fixed `(5,3)` but flipped 4 OTHER cells — all confirmed Heart+thorn from the immediately-prior L37 fix — to now misread as Dark.
- Root cause: whole-cell raw-RGB nearest-match for this specific pair (Dark+thorn / Heart+thorn) is genuinely ambiguous today — the new Dark+thorn point sat almost exactly between the two real clusters, so it "fixed" the reported cell by dragging the decision boundary right through 4 already-correct ones. Unlike L37 (where one candidate signature was simply wrong/mislabeled and removing it left a clear winner), THIS pair has two real, valid categories that are just genuinely close in absolute color space under current lighting — adding more Euclidean anchor points can't fix a boundary problem, it can only move where the boundary cuts.
- Lesson: when a fix works by adding/adjusting a point in a nearest-neighbor color space, don't just re-check the ONE reported cell — re-run the FULL board (or at least every cell of the neighboring/competing type) before considering it done. A fix that satisfies the reported symptom can silently break siblings that were passing moments ago, especially in a shared decision space where every point's neighborhood overlaps every other point's.
- The actual fix: looked for a feature OTHER than raw-RGB proximity that separates the two clusters. Green-minus-blue did, with a large margin (Heart+thorn -7..-9 vs Dark+thorn -47, calibration signatures agree on direction: -34 vs -66) — implemented as a scoped post-match override in `classify()`, applied only when the ordinary match already lands on this exact pair, so it's provably inert for every other type/pair in the table.
- Evidence: full-board `--check` after the real fix shows the reported cell AND all 4 previously-fixed cells simultaneously correct; `--dry` end-to-end solve unaffected.
- Affected files: phone/autospin.js (`classify()` — new g-b discriminator scoped to `type 4/5 && thorn`; the earlier second-signature-point attempt was reverted, not left in place); PROJECT-FACTS P26.

## L39 2026-07-10 An unconditional tiebreaker can override a confidently-correct match — only invoke it when the original signal was actually close
- Symptom: User reported `(0,1)`/`(0,3)` (confirmed Heart) reading as Dark, on the very next board after the L38 g-b discriminator shipped.
- Root cause: the L38 fix applied its g-b override WHENEVER the raw nearest match landed on Dark+thorn or Heart+thorn — with no check on whether the raw match was itself confident. For these two cells the raw distance clearly and correctly favored Heart (7.3 vs 32.7, a huge margin) but g-b measured -29, just past the -25 cutoff meant for the genuinely-ambiguous case from L38 — so a confident, correct primary signal got overridden by a secondary discriminator that was only ever validated against AMBIGUOUS cases, never checked against confident ones.
- Lesson: a tiebreaker/discriminator built to resolve close calls must be GATED on the calls actually being close, not applied unconditionally whenever the two competing categories are involved. Validate a new discriminator against BOTH kinds of case before shipping — the ambiguous ones it's meant to fix, AND confidently-correct ones from before the fix — not just the reported symptom. (Same root pattern as L38 itself: test the fix's effect on already-correct neighbors, not just the reported cell — this time the "neighbor" was a different board captured moments later, which is why it wasn't caught the first time.)
- The fix: compute both `dDark` and `dHeart` explicitly and only invoke the g-b override when `|dDark - dHeart| < 18` — a margin picked from the full set of known live data points (ambiguous cases: gap ~10-12; confident cases: gap ~23-26), leaving comfortable headroom on both sides of the threshold.
- Evidence: full-board `--check` re-run shows `(0,1)`/`(0,3)` now `Heart*` while `(0,2)`/`(5,4)` (unflagged, presumably-correct Dark cells on the same board) are unchanged; `--dry` end-to-end solve completes normally.
- Affected files: phone/autospin.js (`classify()` — g-b override now gated on `Math.abs(dDark - dHeart) < 18`); PROJECT-FACTS P28.
- Affected files: algorithm.js (`BoardSimulator.resolve`'s `hazardPositions`/`hazardViolated`, `DoraSolver`'s `hazardOk` gate on every best-tier, `TargetPlanner`'s route rejection, `solveMaxFirstCombos` threading), phone/autospin.js (removed the wrong `CELL_FLAGS.NO_DISSOLVE` assignment for `c.edgeNoClear`; builds and threads `hazardPositions`; new `BOARD_AFTER_ROW`/`FIRST_WAVE_CLEARED`/self-check `WARNING`/general `ABORT=no-hazard-safe-path` diagnostics), phone/parallel.js (`solveMaxFirstCombosParallel` threading gap fixed); PROJECT-FACTS P22 (marked superseded) + P23 (the corrected model).

## L40 2026-07-10 The L36 shield fix reused FROZEN wholesale, but FROZEN is TOO STRICT for shield — "never dissolve" and "must not ALL dissolve" are different constraints that happen to share the same per-rune travel requirement
- Symptom: none live yet — User caught it by direct correction before a real spin exposed it: "the shielded rune can be dissolve actually. but we can't dissolve all. if board has zero shield rune, the card cannot attack. now we make it like not dissolvable which wrong, treated as frozen."
- Root cause: L36 correctly identified that shield is PER-RUNE (travels with the dragged rune, needs the same in-band board-value trick as FROZEN so it survives arbitrary `Board.swap`/`clone` calls during path construction) — but then reused FROZEN's *behavior* wholesale too (never matches, excluded from every run scan), not just its *travel mechanism*. Those are separable: FROZEN needs BOTH "travels with the rune" AND "never dissolves." Shield only needs the first — it dissolves completely normally, merging into runs with plain same-color cells exactly like any other rune. The actual constraint is a WHOLE-BOARD, PER-COLOR count floor (>=1 shielded survivor after every cascade wave), not a per-cell dissolve ban. Modeling "can't dissolve them all" as "can't dissolve any of them" is a strictly stronger, wrong constraint — it would make the solver refuse perfectly legal boards the real game accepts (any match sweeping a shielded rune while others of that color remain shielded elsewhere).
- Lesson: when an existing mechanism (FROZEN) is the right *carrier* for a new mechanic because they share one specific technical requirement (per-rune value that travels through swaps), don't assume the ENTIRE behavior transfers too. Decompose the requirement first: what has to travel, and separately, what rule actually applies once it's there? Here "travels with the rune" (mechanism) and "never dissolves" (FROZEN's specific rule) got bundled together by borrowing the whole primitive, when only the mechanism half applied to shield — the actual shield rule (per-color survivor-count floor) is a NEW rule needing its own check, not a rule any existing mechanism already implements.
- The fix: kept the in-band board-value trick (still necessary for per-rune tracking through drags) but gave shield its OWN value range (`SHIELD_BASE + baseType`, values 7-12, distinct from `FROZEN=6`) so match-finding treats it as a fully normal rune of its base color (`baseType()`/`isShielded()` helpers normalize wherever raw board values are compared: `findComboGroups`' row/column/flood-fill scans, `pairPotential`, `clearTypeTotals` in both `DoraSolver`/`TargetPlanner`, plus the mirrored scans in `phone/parallel.js` and the CLI-level totals/labels in `phone/autospin.js`). `BoardSimulator.resolve()` now tracks `shieldTotal[type]`/`shieldRemaining[type]` (decremented only when a shielded cell is actually swept into a dissolving group) and sets `shieldViolated` per-color, gated into `DoraSolver`'s three best-tiers (`shieldOk`, mirroring `hazardOk`) and `TargetPlanner`'s route rejection — same "structural exclusion vs. post-hoc rejection" pattern as L34, except here the post-hoc check is a COUNT threshold, not a positional touch.
- A subtlety this surfaces for composition (User explicitly asked to verify against `--first-wave-no`): because shielded runes now count as fully "owed" for `clearTypes` totals (a shielded rune of a demanded color IS expected to eventually dissolve), a clear-all demand that requires dissolving 100% of a color while that color has exactly one shielded rune is a genuine, correctly-detected CONFLICT between two mandatory demands (clear-all needs zero remaining; shield needs >=1 remaining) — `TargetPlanner.solveClearAll()` now correctly returns no solution rather than silently satisfying one demand at the other's expense (verify.js SH6).
- Evidence: verify.js SH1 (shielded runs match/dissolve normally, not blocked), SH2 (shielded+plain merge into one group), SH3 (surviving shielded rune elsewhere on the board correctly avoids a violation), SH4 (DoraSolver never returns a board that zeroes a color's shield count), SH5 (composes correctly with `firstWaveNoTypes`), SH6 (TargetPlanner correctly detects the clear-all/shield conflict as infeasible) — node verify.js all passing, no regressions in the pre-existing 148.
- Affected files: algorithm.js (`SHIELD_BASE`/`baseType`/`isShielded`; `findComboGroups` row/col/flood-fill normalization; `BoardSimulator.resolve`'s `shieldTotal`/`shieldRemaining`/`shieldViolated`; `DoraSolver` constructor `clearTypeTotals`, `pairPotential`, `trappedRequiredCount`, `calculateWeight`, `solve()`'s `shieldOk` gate on all 3 tiers; `TargetPlanner` constructor `clearTypeTotals`, `solve()`/`solveClearAll()` rejection), phone/autospin.js (board encoding now `SHIELD_BASE + type` instead of `FROZEN`; `CLEAR_ALL_TARGET`/clear-all gate counts include shielded cells; `BOARD_AFTER_ROW` label decoding; new `SHIELD_RESULT`/shield `WARNING` diagnostics), phone/parallel.js (`clearTypeTotals` scan fixed the same way); PROJECT-FACTS P24 (superseded) + P30 (the corrected model).

## L41 2026-07-10 Two mandatory demands that share the SAME type can silently contradict each other when a fallback is meant to REPLACE one of them, not compose with it
- Symptom: caught in `--dry` live CLI testing before shipping (not a User report) — `--first-wave-have=w,h` on a board with 3 Waters and 0 Hearts correctly dropped Heart as infeasible and reserved Water, but then ALWAYS aborted with `ABORT=first-wave-have Water not dissolved`, even at beam 2000, on a board where satisfying "clear >=1 Water" alone was trivially easy.
- Root cause: the intended design (confirmed with the User beforehand) was that when a sibling type is dropped as infeasible, the achievable survivors' mandatory "clear >=1 this round" demand is REPLACED by the reserve-floor demand ("keep >=min-run remaining"), not supplemented by it. During implementation, a mid-edit accidentally dropped the line that filters the dead type out of `firstWaveHaveTypes` before copying it into `reserveTypes`, AND separately never cleared `firstWaveHaveTypes` to empty — so the survivor (Water, with exactly 3 total, exactly its own min-run) ended up subject to BOTH constraints simultaneously: "clear >=1" (must dissolve at least 1) and "keep remaining >=3" (must dissolve 0) — mutually exclusive whenever a type's total equals exactly its min-run, making the demand permanently unsatisfiable regardless of beam width.
- Lesson: when a fallback path is designed to REPLACE a demand rather than add a new one alongside it, make the replacement explicit and total (empty out the old demand's list, not just filter it) — leaving stale state in both places, even briefly during a refactor, produces a contradiction that only surfaces as a beam-width-independent MISS, which looks like "just needs a wider beam" but never resolves no matter how wide. Test the interaction with a board where the threshold is EXACT (total == min-run), not just "few" — that's the specific case where "clear some" and "keep some" collide; a board with plenty of the type wouldn't have caught this.
- The fix: when types are dropped, explicitly `reserveTypes = firstWaveHaveTypes.filter(t => !haveDeadSet.has(t))` THEN `firstWaveHaveTypes.length = 0` — the mandatory have-demand is fully absent for this round, only the reserve floor remains active for survivors.
- Evidence: live `--dry` re-test on the exact repro board (3 Water, 0 Heart) now solves cleanly (`RESERVE_RESULT=Water:3/3min ok`, no `FIRST_WAVE_HAVE_RESULT` line at all since the demand is empty this round) instead of aborting at any beam width; node verify.js 171/171 unaffected (the existing FH1-FH4 checks test `reserveTypes`/`resolve()` in isolation and didn't exercise this CLI-level composition bug — a gap worth remembering: unit-level checks on the underlying mechanism don't catch a caller-side wiring mistake in how two demands are supposed to compose).
- Affected files: phone/autospin.js (`--first-wave-have` infeasibility-fallback block — restored the `haveDeadSet` filter and added the explicit `firstWaveHaveTypes.length = 0`); PROJECT-FACTS P32.
